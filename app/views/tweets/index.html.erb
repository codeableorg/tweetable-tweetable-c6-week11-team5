<section class="section-sm">
  <div class="container-sm">
    <p style="color: green"><%= notice %></p>
    <div class="w-full center">
      <h1 class="semibold head-sm">Home</h1>
    </div>

    <div>
      <% @tweets.each do |tweet| %>
        <%= render tweet %>
      <% end %>
    </div>

    <%= link_to "New tweet", new_tweet_path %>
  </div>
</section>